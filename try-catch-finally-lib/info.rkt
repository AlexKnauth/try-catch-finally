#lang info

;; Package Info

(define collection "try-catch-finally")

(define deps '("base"))

(define pkg-desc "A macro for catching exceptions and running teardown operations.")

(define version "0.0")

